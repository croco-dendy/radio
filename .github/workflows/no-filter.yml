name: No Filter Test

on:
  pull_request:
    branches: [main, develop]

jobs:
  no-filter:
    name: No Filter Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Test
        run: echo "No filter workflow triggered!"
