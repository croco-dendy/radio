name: Simple Wave

on:
  pull_request:
    branches: [main, develop]
    paths:
      - 'apps/wave/**'

jobs:
  simple:
    name: Simple Wave Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Test
        run: echo "Simple Wave workflow triggered!"
